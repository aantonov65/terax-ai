FROM node:22-slim

WORKDIR /app
RUN corepack enable

COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile

COPY backend ./backend

ENV NODE_ENV=production
ENV WORKER_CONCURRENCY=6

CMD ["pnpm", "backend:worker"]
