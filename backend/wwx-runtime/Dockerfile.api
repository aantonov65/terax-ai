FROM node:22-slim

WORKDIR /app
RUN corepack enable

COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile

COPY backend ./backend
COPY packages ./packages
COPY engines ./engines

ENV NODE_ENV=production
ENV HOST=0.0.0.0

CMD ["pnpm", "backend:api"]
